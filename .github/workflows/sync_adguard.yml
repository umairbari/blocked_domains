name: Convert Blocklist to AdGuard Format

on:
  push:
    paths:
      - 'list.txt' # Only runs when one of these file changes
      - 'allowed.txt' # Only runs when one of these file changes
      - 'doh_blocklist.txt' # Only runs when one of these file changes
  workflow_dispatch: # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to push the new file back
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run Converter
        run: python convert.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add adguard_list.txt adguard_allowed.txt adguard_doh_blocklist.txt
          git commit -m "Auto-update AdGuard lists" || exit 0
          git push
